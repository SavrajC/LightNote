const Content = ({content, setContent}) => {
    console.log(content);
    console.log(setContent)
    return (
        <div className="content">
            
        </div>
    );
};

export default Content